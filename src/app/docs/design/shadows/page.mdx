import { OG_IMAGE } from "@/app/utils/constants";
import { Grid, IconFigma } from "cherry-styled-components/src/lib";
import { PreviewTile } from "@/app/components/preview-tile";
import { Box } from "@/app/components/box";
import { Shadows } from "@/app/components/shadows";
import { IconFile } from "@/app/components/icons";

export const metadata = {
	title: "Shadows - Design - Cherry Design System",
	openGraph: {
		title: "Shadows - Design - Cherry Design System",
		images: OG_IMAGE,
	},
};

# Shadows - Design

Cherry offers several shadow variations that allow you to create elevations and add depth to your interfaces. Shadows play a crucial role in enhancing the visual hierarchy of elements, providing a sense of depth and realism.

<Grid $xsGap={20} $xsCols={1} $lgCols={3}>
	<PreviewTile $bgColor="#FFFFFF" $title="Shadow" $subTitle="xs" $mq="XS">
		<Shadows $size="xs" />
	</PreviewTile>
	<PreviewTile $bgColor="#FFFFFF" $title="Shadow" $subTitle="SM" $mq="sm">
		<Shadows $size="sm" />
	</PreviewTile>
	<PreviewTile $bgColor="#FFFFFF" $title="Shadow" $subTitle="MD" $mq="md">
		<Shadows $size="md" />
	</PreviewTile>
	<PreviewTile $bgColor="#FFFFFF" $title="Shadow" $subTitle="LG" $mq="lg">
		<Shadows $size="lg" />
	</PreviewTile>
	<PreviewTile $bgColor="#FFFFFF" $title="Shadow" $subTitle="XL" $mq="lg">
		<Shadows $size="lg" />
	</PreviewTile>
</Grid>

Shadows can be found in the Figma Template in the ☁️ **Shadows** page.

<Box
	$icon={<IconFile />}
	$iconLink={<IconFigma />}
	$title="Shadows"
	href="https://www.figma.com/community/file/943862931766586094/cherry-design-system"
	target="_blank"
/>
