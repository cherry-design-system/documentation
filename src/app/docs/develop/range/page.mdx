import { OG_IMAGE } from "@/app/utils/constants";
import {
	Space,
	Grid,
	Range,
} from "cherry-styled-components/src/lib";
import { PreviewTile } from "@/app/components/preview-tile";
import { Box } from "@/app/components/box";
import { IconFile, IconGitHub } from "@/app/components/icons";
import { Table, TableRow, TableHead, TableData } from "@/app/components/table";

export const metadata = {
	title: "Range - Develop - Cherry Design System",
	openGraph: {
		title: "Range - Develop - Cherry Design System",
		images: OG_IMAGE,
	},
};

# Range - Develop

Range inputs shares the same properties as Input component, it just allows users to Range a value from a range of values. Range can have an error or success state to provide feedback to the user.

Keep in mind that the **\<input>** element inherently includes native browser properties like onChange. Additionally, Cherry UI components introduce custom properties that always begin with a **$** to distinguish them from native component props.

```jsx
import React from "react";
import { Range } from "cherry-styled-components";

export default function Page() {
  return (
    <Range />
  )
}
```

<Space $size={20} />

<PreviewTile $title="Range" $subTitle="Default">
	<Range />
</PreviewTile>

You can add a label by adding a **$label** prop.

```jsx
import React from "react";
import { Range } from "cherry-styled-components";

export default function Page() {
  return (
    <Range $label="Label" $fullWidth />
  )
}
```
<Space $size={20} />

<PreviewTile
	$title="Range"
	$subTitle="Full width with Label"
	$isLonger
>
	<Range $label="Label" $fullWidth />
</PreviewTile>

Here is an example of a Range with success and error state.

```jsx
import React from "react";
import { Range } from "cherry-styled-components";

export default function Page() {
  return (
    <>
      <Range $success />
      <Range $error />
    </>
  )
}
```
<Space $size={20} />

<Grid $gap={20} $lgCols={2}>
	<PreviewTile
		$title="Range"
		$subTitle="Success"
		$isLonger
	>
		<Range $success />
	</PreviewTile>
	<PreviewTile
		$title="Range"
		$subTitle="Error"
		$isLonger
	>
		<Range $error />
	</PreviewTile>
</Grid>

## Properties

<Table>
	<TableRow>
		<TableHead>Property</TableHead>
		<TableHead>Description</TableHead>
	</TableRow>
	<TableRow>
		<TableData $code>$label?</TableData>
		<TableData>string</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$size?</TableData>
		<TableData>"default" | "big"</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$error?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$success?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$fullWidth?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>theme?</TableData>
		<TableData>Theme</TableData>
	</TableRow>
</Table>

Explore the source code on GitHub:

<Box
	$icon={<IconFile />}
	$iconLink={<IconGitHub />}
	$title="Range"
	href="https://github.com/cherry-design-system/styled-components/blob/main/src/lib/range.tsx"
	target="_blank"
/>
