import { OG_IMAGE } from "@/app/utils/constants";
import {
	Space,
	Grid,
	Select,
	IconGitHub,
} from "cherry-styled-components/src/lib";
import { PreviewTile } from "@/app/components/preview-tile";
import { Box } from "@/app/components/box";
import { IconFile } from "@/app/components/icons";
import { Table, TableRow, TableHead, TableData } from "@/app/components/table";

export const metadata = {
	title: "Select - Cherry Design System",
	openGraph: {
		title: "Select - Cherry Design System",
		images: OG_IMAGE,
	},
};

# Select

Select shares the same properties as Input component of type text, it just allows for selecting from a list instead of typing. And you can pass children to it. Select can have an error or success state to provide feedback to the user.

Keep in mind that the **\<select>** element inherently includes native browser properties like onChange. Additionally, Cherry UI components introduce custom properties that always begin with a **$** to distinguish them from native component props.

```jsx
import React from "react";
import { Select } from "cherry-styled-components";

export default function Page() {
  return (
    <Select>
      <option value="1">Option 1</option>
      <option value="2">Option 2</option>
      <option value="3">Option 3</option>
    </Select>
  )
}
```

<Space $size={20} />

<PreviewTile $bgColor="#FFFFFF" $title="Select" $subTitle="Default">
	<Select>
		<option value="1">Option 1</option>
		<option value="2">Option 2</option>
		<option value="3">Option 3</option>
	</Select>
</PreviewTile>

You can add a label by adding a **$label** prop.

```jsx
import React from "react";
import { Select } from "cherry-styled-components";

export default function Page() {
  return (
    <Select $label="Label" $fullWidth>
      <option value="1">Option 1</option>
      <option value="2">Option 2</option>
      <option value="3">Option 3</option>
    </Select>
  )
}
```
<Space $size={20} />

<PreviewTile
	$bgColor="#FFFFFF"
	$title="Select"
	$subTitle="Full width with Label"
	$isLonger
>
	<Select $label="Label" $fullWidth>
		<option value="1">Option 1</option>
		<option value="2">Option 2</option>
		<option value="3">Option 3</option>
	</Select>
</PreviewTile>

Here is an example of a Select with success and error state.

```jsx
import React from "react";
import { Select } from "cherry-styled-components";

export default function Page() {
  return (
    <>
      <Select $success>
        <option value="1">Yay</option>
      </Select>
      <Select $error>
        <option value="1">Ops</option>
      </Select>
    </>
  )
}
```
<Space $size={20} />

<Grid $gap={20} $lgCols={2}>
	<PreviewTile
		$bgColor="#FFFFFF"
		$title="Select"
		$subTitle="Success"
		$isLonger
	>
		<Select $success>
			<option value="1">Yay</option>
		</Select>
	</PreviewTile>
	<PreviewTile
		$bgColor="#FFFFFF"
		$title="Select"
		$subTitle="Error"
		$isLonger
	>
		<Select $error>
			<option value="1">Ops</option>
		</Select>
	</PreviewTile>
</Grid>

## Properties

<Table>
	<TableRow>
		<TableHead>Property</TableHead>
		<TableHead>Description</TableHead>
	</TableRow>
	<TableRow>
		<TableData $code>$label?</TableData>
		<TableData>string</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$size?</TableData>
		<TableData>"default" | "big"</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$error?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$success?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$fullWidth?</TableData>
		<TableData>boolean</TableData>
	</TableRow>
	<TableRow>
		<TableData $code>$theme?</TableData>
		<TableData>Theme</TableData>
	</TableRow>
</Table>

Explore the source code on GitHub:

<Box
	$icon={<IconFile />}
	$iconLink={<IconGitHub />}
	$title="Select"
	href="https://github.com/cherry-design-system/styled-components/blob/main/src/lib/select.tsx"
	target="_blank"
/>
